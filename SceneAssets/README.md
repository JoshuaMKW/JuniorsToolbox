In order to allow Junior's Toolbox to intelligently verify the contents of a scene and insert missing assets,
the user should place a valid SceneAssets pack into this folder.

This can be done by extracting the SceneAssets pack into this folder directly.